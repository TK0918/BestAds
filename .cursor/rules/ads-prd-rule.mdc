---
description: PRD output must follow PRD/_template.md and use "略" for non-applicable sections
alwaysApply: true
---
# PRD 输出规则

## 适用范围

- 当用户要求输出 PRD 时, 必须使用 `PRD/_template.md` 的目录结构.
- 保留模板目录层级和标题名称, 不得擅自改名或删减目录.
- 一级章节使用 `# 标题`(无序号); 二级使用 `##`; Tab 下功能小节使用 `####`; 其余 Tab 内小节使用 `###`.
- 组织方式为「页面/功能域 × 业务要素」, 禁止回退到旧版「系统 × 模块 × 固定五段式」写法.

## 填写原则

- 有明确需求内容的目录: 完整填写.
- 暂无内容或本期不需要的目录: 保留目录并写 `略`.
- 用户明确说明不需要的部分(例如埋点需求): 必须写 `略`, 不得补充扩展内容.
- **单页**「页面说明」按 **页面信息 → 页面概述 → 筛选字段 → 列表字段 → 功能说明** 顺序填写.
- **多 Tab 页**「页面说明」按 **页面信息 → 页面概述 → 各 Tab 树结构** 顺序填写; 每个 Tab 使用 `## Tab X: {名称}`, 其下依次写 **Tab 说明 → 筛选字段 → 列表字段 → 功能说明**(`###`), 功能项使用 `####`. **禁止**把多个 Tab 的筛选/列表/功能拆成并列大章(如「Tab A 筛选」「Tab B 筛选」集中放在一起), 避免读者跨 Tab 对照.
- 「功能说明」下按业务操作拆分, 每项写清作用、交互、表单字段(无表单则写 `无`).

## 按需章节(非全量强制)

| 章节 | 何时写 | 何时写 `略` |
| --- | --- | --- |
| 核心原则 | 有跨模块统一口径 | 无 |
| 专业名词说明 | 全文有需统一定义的业务/技术术语 | 无特殊术语 |
| 与现有功能关系 | 与存量功能有关联 | 纯独立新页 |
| 业务流程 | 跨角色/跨系统/审核流 | 简单 CRUD/单页查询 |
| 状态机 | 对象有明确状态流转 | 无业务状态 |
| 筛选字段 | 有筛选/查询 | 无筛选 |
| 列表字段 | 有列表/表格展示 | 无列表 |
| 功能说明 | 有可操作功能 | 纯展示页 |

## 内容要求

- 必须包含表格; 涉及流程或状态流转时使用 Markdown + Mermaid.
- 页面结构/Tab 说明: **只用文字说明分区**, 不写 Mermaid.
- 业务流程: 需有开始/结束, 判断分支需有是/否, 并包含异常路径.
- 状态机: 需给出状态流转图和状态操作表.
- 表格字段名和业务口径必须与用户确认内容一致, 不得杜撰未确认规则.
- 禁止重复: 筛选/列表/功能不在多处重复定义; 同一业务链路不在「业务流程」与「功能说明」中各写一遍完整流程.

## 风格规范

- 全文使用简体中文.
- 标点使用英文标点.
- 文档路径默认放在 `PRD/` 目录, 文件名建议 `{feature}-prd.md` 或 `V x.xx {名称}.md`.

## 执行优先级

- 若用户在当前会话给出新的模板或覆盖要求, 以用户最新要求为准.
- 若旧模板和新模板冲突, 直接采用用户最新模板(`PRD/_template.md`).
