---
description: PRD output must follow user template and use "略" for non-applicable sections
alwaysApply: true
---
# PRD 输出规则

## 适用范围

- 当用户要求输出 PRD 时, 必须使用用户提供的 PRD 模板结构.
- 保留模板目录层级和编号, 不得擅自改名或删减目录.

## 填写原则

- 有明确需求内容的目录: 完整填写.
- 暂无内容或本期不需要的目录: 保留目录并写 `略`.
- 用户明确说明不需要的部分(例如埋点需求): 必须写 `略`, 不得补充扩展内容.

## 内容要求

- 必须包含流程图和表格, 使用 Markdown + Mermaid.
- 流程图需有开始/结束, 判断分支需有是/否, 并包含异常路径.
- 状态机需给出状态流转图和状态操作表.
- 表格字段名和业务口径必须与用户确认内容一致, 不得杜撰未确认规则.

## 风格规范

- 全文使用简体中文.
- 标点使用英文标点.
- 文档路径默认放在 `PRD/` 目录.

## 执行优先级

- 若用户在当前会话给出新的模板或覆盖要求, 以用户最新要求为准.
- 若旧模板和新模板冲突, 直接采用用户最新模板.
