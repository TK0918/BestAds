#!/usr/bin/env python3
"""Rewrite V2.23 PRD sections for v0.4 simplified 3-layer model."""
from pathlib import Path

PRD = Path(__file__).resolve().parents[2] / "PRD/V 2.23 介绍人和吐点（第二版）.md"
text = PRD.read_text(encoding="utf-8")

# Insert v0.4 revision after v0.3 row
if "0.4" not in text.split("修订历史")[1][:800]:
    text = text.replace(
        "| 0.3 | 2026-05-30 | M | - | 待定 | 增补 **吐点方案按时间段分段（方案 A）** 与 **L2-Override 客户调价矩阵（方案 B）**； 结算优先级、比例来源、Tab B 配置与生效预览同步升级 | 业务场景：同一 I–C 关系 Q1 走介绍人默认、Q2 起需仅对客户 C 调价或切换整套独立价目， v0.1~0.2 静态吐点方案无法表达 |\n",
        "| 0.3 | 2026-05-30 | M | - | 待定 | 增补 **吐点方案按时间段分段（方案 A）** 与 **L2-Override 客户调价矩阵（方案 B）**； 结算优先级、比例来源、Tab B 配置与生效预览同步升级 | 业务场景：同一 I–C 关系 Q1 走介绍人默认、Q2 起需仅对客户 C 调价或切换整套独立价目， v0.1~0.2 静态吐点方案无法表达 |\n"
        "| 0.4 | 2026-05-30 | M | - | 待定 | **简化三层模型**： 特殊吐点(L1) / 客户基础吐点(L2， 含时间分段+计价模式+media×type 矩阵) / 介绍人吐点(L3)； **废弃** v0.3 吐点方案段、L2-Override、L2-Full 分表； 比例来源改为四种 | 降低运营理解成本； 业务不存在「全线同一比例」， 矩阵均按媒体×账户类型 |\n",
    )

START = "# 与 V2.20 变更对照"
END = "# 客户端"
idx_s = text.index(START)
idx_e = text.index(END)
new_middle = r'''# 与 V2.20 变更对照

> 本章仅描述 **相对已上线 V2.20 的关键差异**。未列出的能力（结算批次、双审、Posted 不可变更、钱包入账、交易明细 INTRODUCER_REBATE、客户端 7.1 等）**保持 V2.20 口径不变**。

> **层级编号说明（v0.4）**： 下文 **L1/L2/L3 为业务讲解顺序**（L1 最高优先级）， 与 v0.1~0.3 文档中「L1=介绍人矩阵」的技术编号不同。 对应关系：**L1 特殊吐点** ≈ 原账户特殊； **L2 客户基础吐点** ≈ 原关系级基础 + 按季分段； **L3 介绍人吐点** ≈ 原介绍人默认矩阵。

## 变更总览

| 维度 | V2.20（已上线） | V2.23（v0.4） |
| --- | --- | --- |
| 吐点配置层级 | 关系级「基础吐点」+ 账户级「特殊吐点」 | **三层**：**L3 介绍人吐点** + **L2 客户基础吐点** + **L1 特殊吐点** |
| 介绍人标准价目 | 关系级基础吐点（难以表达 media×type） | **L3**：介绍人 + 日期 + **媒体 × 账户类型** → 比例 |
| 客户关系默认 | 关系级基础吐点（可按时间分段） | **L2 时间分段** + **计价模式**：「使用介绍人吐点」或「客户基础吐点」； 后者配 **media×type 矩阵** |
| 单户例外 | 特殊吐点 | **L1 特殊吐点**（不变语义， 优先级最高） |
| 按季演进（如 Q1/Q2） | 改关系基础吐点分段 | **L2 增一段**： Q1「使用介绍人吐点」、Q2「客户基础吐点」+ 矩阵， **无需** 独立方案/Override/Full 三套表 |
| 结算优先级 | 特殊 > 基础 > 0 | **L1 特殊 > L2（按模式分支）> L3 > 0** |
| 比例来源 | 基础 / 特殊 | **特殊吐点 / 客户基础 / 介绍人吐点 / 无规则** |
| Admin 结构 | 3 Tab | 介绍人列表维护 **L3**； 关系 Tab 维护 **L2 分段 + L2 矩阵 + L1 特殊** |
| 已 Posted 历史季度 | — | **不回算** |

## 变更原因（业务侧）

- 商务按 **媒体 × 账户类型** 谈价， 不存在「对客户全线同一比例」而不区分媒体/类型。
- v0.3 的吐点方案段 + L2-Override + L2-Full **概念过多**， 运营难理解； v0.4 收敛为与口语一致的 **介绍人吐点 / 客户基础 / 特殊** 三层。
- 典型诉求： 介绍人 I 发展客户 C， **Q1** 用 I 的 **介绍人吐点（L3）**； **Q2** 起对该客户改用 **客户基础吐点（L2）** 并单独维护 media×type 矩阵， **不影响** I 的其他客户。

## 迁移原则（实施侧）

| 原则 | 说明 |
| --- | --- |
| 不回算 Posted | 已完结结算单不调整 |
| 原介绍人默认矩阵 | → **L3 介绍人吐点** |
| 原关系基础吐点 / 方案继承段 | → **L2 分段**， 模式=使用介绍人吐点 |
| 原 Override / 独立方案+Full | → **L2 分段**， 模式=客户基础吐点 + 矩阵行 |
| 原账户特殊 | → **L1 特殊吐点** |

---

# 需求背景与目标

## 需求背景

- V2.20 已交付结算闭环， 但吐点配置无法表达介绍人级 **媒体×账户类型** 价目表及按季切换客户关系计价方式。
- 客户-广告账户绑定 **复用既有能力**。

## 需求目标

- 建立 **三层吐点模型**（均为 **媒体 × 账户类型 × 时间段** 矩阵， 无全线统一比例）：
  - **L3 介绍人吐点**： 介绍人 + 日期；
  - **L2 客户基础吐点**： 介绍人 + 客户 + 日期， 含 **计价模式** 与可选矩阵；
  - **L1 特殊吐点**： 介绍人 + 客户 + 账户 + 日期。
- **L2 计价模式**： `使用介绍人吐点`（该段时间走 L3）、`客户基础吐点`（该段时间走 L2 矩阵）。
- 结算：**L1 > L2（按日分段模式）> L3 > 0**； 比例来源四种。
- 其余（客户端 7.1、双审、Posted、多介绍人独立）**延续 V2.20**。

## 核心原则

| 规则 | 说明 |
| --- | --- |
| 覆盖不叠加 | 同一自然日、同一（介绍人， 客户， 账户）仅一个比例来源 |
| L2 按日解析 | 以自然日 **D** 取 `(I,C)` 唯一命中的 **L2 时间分段** 及其计价模式 |
| 矩阵维度 | L2/L3 矩阵均为 **媒体 × 账户类型**， 须账户主数据匹配 |
| 闸门不变 | 账户归属客户 + 介绍人绑定客户 |
| Posted 不回算 | 配置变更仅影响新批次 |
| 模式互斥 | 同一日 L2 分段只能有一种模式； `客户基础吐点` 日 **只读 L2 矩阵**， 不读 L3 |

## 与现有功能关系

| 模块/页面 | 关系 |
| --- | --- |
| V2.20 介绍人和吐点 | 升级吐点配置与取值； 结算/客户端复用 |
| 账户管理 | 提供 media、account_type |

---

# 专业名词说明

| 名词 | 说明 |
| --- | --- |
| L3 介绍人吐点 | 挂在 **介绍人**； **媒体 × 账户类型 × 时间段 → 比例**； 当 L2 分段为「使用介绍人吐点」或 L2 未覆盖时按规则取 L3 |
| L2 客户基础吐点 | 挂在 **介绍人-客户关系**； 含 **时间分段** 与 **计价模式** |
| L2 计价模式 | `使用介绍人吐点`： 该段时间结算按 **L3** 匹配； `客户基础吐点`： 该段时间按 **L2 矩阵**（media×type）匹配 |
| L2 客户基础矩阵 | 仅当分段模式=`客户基础吐点` 时维护； 字段同 L3 矩阵 |
| L1 特殊吐点 | 挂在 **介绍人-客户-账户**； 优先级最高， 覆盖 L2/L3 |
| 比例来源 | **特殊吐点 / 客户基础 / 介绍人吐点 / 无规则** |
| 生效预览 | 图例对应上述四种来源 |

## 典型业务场景

| 场景 | 配置 | Q1 来源 | Q2 来源 |
| --- | --- | --- | --- |
| Q1 用介绍人价目、Q2 仅改客户 C | L2： Q1 段=使用介绍人吐点； Q2 段=客户基础吐点 + 矩阵 | 介绍人吐点 | 客户基础 |
| 客户长期独立价目 | L2 长期段=客户基础吐点 + 矩阵 | 客户基础 | 客户基础 |
| 某账户例外 | L1 特殊吐点 | 特殊吐点 | 特殊吐点 |
| I 名下所有人涨价 | 只改 **L3** 矩阵分段 | 介绍人吐点 | 介绍人吐点（若 L2 仍为使用介绍人吐点） |

---

# 业务流程

## 结算主流程（变更部分）

```mermaid
flowchart TD
  S[季度结束或手动发起] --> U[取日消耗]
  U --> G1{闸门A}
  G1 -- 否 --> SKIP[跳过]
  G1 -- 是 --> G2{闸门B}
  G2 -- 否 --> SKIP
  G2 -- 是 --> P1{L1 特殊吐点命中?}
  P1 -- 是 --> CALC[消耗×比例]
  P1 -- 否 --> P2{L2 当日分段}
  P2 -- 无分段 --> P4{L3 介绍人吐点命中?}
  P2 -- 使用介绍人吐点 --> P4
  P2 -- 客户基础吐点 --> P3{L2 矩阵命中?}
  P3 -- 是 --> CALC
  P3 -- 否 --> ZERO[无规则]
  P4 -- 是 --> CALC
  P4 -- 否 --> ZERO
  CALC --> AGG[聚合]
  ZERO --> AGG
  SKIP --> AGG
  AGG --> AUDIT[审核入账]
```

| 步骤 | 角色 | 输入 | 输出 |
| --- | --- | --- | --- |
| 配置 L3 | 运营 | 介绍人、media、type、比例、时间 | 介绍人吐点价目表 |
| 建关系 + L2 首段 | 运营 | 关系期、首段计价模式 | 关系 + L2 分段 |
| 配置 L2 矩阵 | 运营 | 模式=客户基础吐点 的时段内 matrix | 客户基础矩阵 |
| 配置 L1 | 运营 | 单账户例外 | 特殊吐点 |

## 配置维护流程

```mermaid
flowchart TD
  A[介绍人列表: L3 介绍人吐点] --> B[新建关系 + L2 首段]
  B --> C{首段模式}
  C -- 使用介绍人吐点 --> D[可选 L1 特殊]
  C -- 客户基础吐点 --> E[维护 L2 矩阵]
  E --> D
  D --> F[生效预览]
```

---

# 状态机

## 介绍人身份

与 V2.20 一致。 停用后不可新建关系/吐点； 存量配置保留。

## 关系绑定

| 状态 | 可执行操作 | 约束 |
| --- | --- | --- |
| Bound | 维护 L2 时间分段 | 段不重叠， 落在关系有效期内 |
| Bound | 维护 L2 矩阵 | 仅 `客户基础吐点` 时段； 矩阵行 media×type 校验同 L3 |
| Bound | 维护 L1 特殊吐点 | 同 V2.20 |

## L1 特殊吐点

Enabled / Disabled， 与 V2.20 一致。

---

# 需求范围

## 本期包含

| 序号 | 范围项 | 优先级 |
| --- | --- | --- |
| 1 | L3 介绍人吐点矩阵 | P0 |
| 2 | L2 客户基础时间分段 + 计价模式 | P0 |
| 3 | L2 客户基础矩阵（media×type） | P0 |
| 4 | L1 特殊吐点 | P0 |
| 5 | 结算引擎（四层来源） | P0 |
| 6 | 生效预览 | P0 |
| 7 | 迁移与结算 Tab | P0 |

## 暂不包含

| 项目 | 说明 |
| --- | --- |
| media/type 通配符 `*` | 二期 |
| v0.3 吐点方案 / Override / Full | **废弃**， 由 v0.4 替代 |
| 埋点 | 略 |

---

# 页面说明

## 页面信息

| 项目 | 内容 |
| --- | --- |
| 路径 | `admin-system/main-functions/introducer-spitpoint.html` |
| 角色 | 运营、财务 |

## 页面概述

| Tab | 职责 |
| --- | --- |
| 介绍人列表 | 身份 + **介绍人吐点（L3）** 入口 |
| 关系与吐点 | 关系 + **L2 客户基础（分段+矩阵）** + **L1 特殊** + 预览 |
| 吐点结算 | 同 V2.20， 比例来源四种 |

---

## Tab A： 介绍人列表

与 V2.20 一致； 操作 **「介绍人吐点」** 进入 L3 矩阵子页（媒体、账户类型、比例、起止、启用/停用）。 校验： 同介绍人同 media×type 的 Enabled 段不重叠。

---

## Tab B： 介绍人关系与吐点

### Tab 说明

- **关系列表**： 关系信息 + L2 分段数、L2 矩阵段数、L1 特殊条数。
- **吐点规则子页**： 关系头 + **L2 客户基础时间分段** + **L2 客户基础矩阵** + **L1 特殊吐点** + **生效预览**。

### 筛选字段

与 V2.20 一致； 增加 **L2 计价模式** 筛选（全部 / 使用介绍人吐点 / 客户基础吐点）。

### 列表字段

| 字段 | 说明 |
| --- | --- |
| 关系生效期 | 同 V2.20 |
| 当前 L2 模式摘要 | 如「Q1 介绍人吐点 → Q2 客户基础」 |
| L2 时间分段数 | Enabled 分段条数 |
| L2 矩阵段数 | 客户基础矩阵 Enabled 条数 |
| L1 特殊条数 | 同前 |

### 功能说明

#### 新增关系

| 字段 | 说明 |
| --- | --- |
| 首段 L2 计价模式 | 单选：`使用介绍人吐点` / `客户基础吐点`； 保存时创建首条 L2 时间分段， 起日=关系生效开始 |
| 首段为「客户基础吐点」 | 保存后引导配置 L2 矩阵（至少一段， 允许 0%） |

#### L2 客户基础时间分段

| 字段 | 说明 |
| --- | --- |
| 计价模式 | `使用介绍人吐点` / `客户基础吐点` |
| 生效开始/结束 | 同矩阵分段； 长期有效可选 |
| 配置状态 | 启用/停用 |

**校验**： 同一关系 Enabled 分段时间不重叠； 建议覆盖关系有效期； 空档日预览标黄。

**交互**： 模式=`客户基础吐点` 时， 展示或跳转 **L2 矩阵** 维护区； 模式=`使用介绍人吐点` 时， 无需矩阵。

#### L2 客户基础矩阵

| 项目 | 说明 |
| --- | --- |
| 作用 | 仅在存在 **客户基础吐点** 的 L2 分段生效期内参与结算 |
| 字段 | 媒体、账户类型、吐点比例、生效起止、启用/停用（**同 L3**） |
| 校验 | 同 L3； 矩阵行生效期 **建议落在** 对应 L2 分段内 |

#### L1 特殊吐点

与 V2.20 特殊吐点一致； 文案 **特殊吐点**； 优先级最高。

#### 生效预览

图例：**特殊吐点 / 客户基础 / 介绍人吐点 / 无规则**。 算法与结算引擎一致。

---

## Tab C： 吐点结算

与 V2.20 一致； 详情 CSV 比例来源：**特殊吐点、客户基础、介绍人吐点、无规则**。

---

# 结算引擎（V2.23 v0.4）

## 取数源

- INTRODUCER_SPIT（L3）
- CUSTOMER_BASE_SPIT_PERIOD（L2 分段， 含计价模式）
- CUSTOMER_BASE_SPIT_MATRIX（L2 矩阵， Enabled）
- ACCOUNT_SPECIAL_SPIT（L1）
- REFERRER_BINDING、CUSTOMER_AD_ACCOUNT_BINDING、账户 media/type

## 逐日取值

对每个 `(I, C, A, D)`：

**闸门**（同 V2.20）

**比例**（命中即停）：

| 优先级 | 条件 | 比例来源 |
| --- | --- | --- |
| P1 | L1 特殊命中 | 特殊吐点 |
| P2 | L2 当日分段模式=客户基础吐点 且 L2 矩阵命中 | 客户基础 |
| P3 | L2 当日分段模式=使用介绍人吐点 且 L3 命中 | 介绍人吐点 |
| P4 | 无 L2 分段 且 L3 命中 | 介绍人吐点 |
| P5 | 未命中 | 无规则 |

> 有 L2 分段且模式=客户基础吐点 时， **不回退 L3**（即使矩阵未配也为无规则）。

**金额**： `round(日消耗×比例, 2)` USD。

## 异常

| 情况 | 处理 |
| --- | --- |
| 缺 media/type | 无法匹配矩阵； 预览标红 |
| 关系期无 L2 分段 | 尝试 P4（L3）； 仍无则无规则 |

---

# 交易明细

略（同 V2.20， 比例来源枚举更新）。

---

# 角色与权限

运营维护 L3/L2/L1； 财务审结算。

---

# 上线与迁移

| 阶段 | 动作 |
| --- | --- |
| 1 | 摸底 media×type 价目 |
| 2 | 导入 L3 |
| 3 | 关系 L2 分段 + 矩阵映射旧基础吐点/Override/Full |
| 4 | L1 收敛 |
| 5 | 双跑验证后切换 |

---

# 埋点需求

略

---

# 附录 A： 数据采集表

| Sheet | 内容 |
| --- | --- |
| 1 | 介绍人身份 |
| 2 | L3 介绍人吐点矩阵 |
| 3 | L2 客户基础时间分段（含计价模式） |
| 4 | L2 客户基础矩阵 |
| 5 | L1 特殊吐点 |

---

# 附录 B： 实体关系

```mermaid
erDiagram
  INTRODUCER_PROFILE ||--o{ INTRODUCER_SPIT : "L3 介绍人吐点"
  INTRODUCER_PROFILE ||--o{ REFERRER_BINDING : "关系"
  REFERRER_BINDING ||--o{ CUSTOMER_BASE_SPIT_PERIOD : "L2 时间分段"
  REFERRER_BINDING ||--o{ CUSTOMER_BASE_SPIT_MATRIX : "L2 矩阵"
  REFERRER_BINDING ||--o{ ACCOUNT_SPECIAL_SPIT : "L1 特殊"
  CUSTOMER ||--o{ REFERRER_BINDING : "被介绍客户"
  AD_ACCOUNT ||--o{ ACCOUNT_SPECIAL_SPIT : "账户"
```

'''

text = text[:idx_s] + new_middle + text[idx_e:]
PRD.write_text(text, encoding="utf-8")
print("PRD OK", PRD)
